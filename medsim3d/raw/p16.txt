ible=true;avatarStatus.textContent='Avatar simple · sin conexión/modelo';avatarStatus.className='avatar-status warn'}}

const controls=new OrbitControls(camera,renderer.domElement);controls.enablePan=false;controls.enableDamping=true;controls.dampingFactor=.08;controls.minPolarAngle=1.18;controls.maxPolarAngle=1.5;
let currentView='full';const desiredCamPos=new THREE.Vector3();const desiredTarget=new THREE.Vector3();
function getSubjectBox(){if(activeAvatar&&activeAvatar.root.visible){const box=new THREE.Box3().setFromObject(activeAvatar.root);const size=new THREE.Vector3();const center=new THREE.Vector3();box.getSize(size);box.getCenter(center);return{box,size,center,min:box.min,max:box.max}}const min=new THREE.Vector3(-.55,floorY,-.2),max=new THREE.Vector3(.55,floorY+1.72,.2),size=new THREE.Vector3(),center=new THREE.Vector3();size.subVectors(max,min);center.addVectors(min,max).multiplyScalar(.5);return{box:null,size,center,min,max}}
function computeView(mode='full'){const s=getSubjectBox();let targetY=s.min.y+s.size.y*.58;let camY=targetY+.18;let dist=isMobile?2.45:3.0;let minD=dist*.82,maxD=dist*1.35;if(mode==='face'){targetY=s.min.y+s.size.y*.86;camY=targetY+.02;dist=isMobile?1.18:1.35;minD=dist*.8;maxD=dist*1.4}else if(mode==='chest'){targetY=s.min.y+s.size.y*.69;camY=targetY+.10;dist=isMobile?1.55:1.85;minD=dist*.82;maxD=dist*1.38}else if(mode==='abdomen'){targetY=s.min.y+s.size.y*.49;camY=targetY+.12;dist=isMobile?1.72:2.08;minD=dist*.84;maxD=dist*1.35}else{targetY=s.min.y+s.size.y*.56;camY=targetY+.24;dist=isMobile?2.45:3.0;minD=dist*.82;maxD=dist*1.35}return{pos:new THREE.Vector3(0,camY,s.center.z+dist),target:new THREE.Vector3(0,targetY,s.center.z),min:minD,max:maxD}}
function setView(mode='full',immediate=false){currentView=['face','chest','abdomen','full'].includes(mode)?mode:'full';viewButtons.forEach(btn=>btn.classList.toggle('active',btn.dataset.view===currentView));const frame=computeView(currentView);desiredCamPos.copy(frame.pos);desiredTarget.copy(frame.target);controls.minDistance=frame.min;controls.maxDistance=frame.max;if(immediate){camera.position.copy(desiredCamPos);controls.target.copy(desiredTarget);controls.update();}}
viewButtons.forEach(btn=>btn.addEventListener('click',()=>setView(btn.dataset.view,true)));
setView('full',true);
function applyVisual(){if(!caseData)return;headMat.color.setHex(caseData.visual.skin);fallback.rotation.set(caseData.visual.guarding ? .08 : 0,-.1,0);leftArm.rotation.z=caseData.visual.guarding ? -.72 : -.15;rightArm.rotation.z=caseData.visual.guarding ? .72 : .15;talkUntil=performance.now()/1000+Math.max(1.1,Math.min(3.5,caseData.opening.length*.045));updateClues();ensureAvatar(caseData.sex)}
let elapsed=0,lastTs=performance.now();function animate(ts){requestAnimationFrame(animate);const delta=Math.min(.05,(ts-lastTs)/1000);lastTs=ts;elapsed+=delta;if(caseData){advanceEmergency(performance.now()/1000);const t=elapsed,v=caseData.visual,breath=Math.sin(t*v.breathingRate);torso.scale.y=1+breath*v.breathDepth;fallback.rotation.z=v.restless?Math.sin(t*2.1)*.02:0;head.rotation.z=(v.headache?-.06:0)+Math.sin(t*.8)*.015;head.rotation.x=v.headache ? .06 : 0;if(treated)treatmentBlend=Math.min(1,treatmentBlend+delta*.22);const eff=treated?(1-.55*treatmentBlend):1,stageBoost=caseData.emergency&&!stabilized&&!treated?1+emergencyStage*.22:1;if(activeAvatar&&activeAvatar.root.visible){const s=activeAvatar.baseScale,bd=v.breathDepth*eff*stageBoost;activeAvatar.model.scale.set(s*(1+breath*bd*.025),s*(1+breath*bd*.015),s*(1+breath*bd*.035));activeAvatar.root.rotation.z=(v.restless||emergencyStage>0)?Math.sin(t*2.15)*.008*eff*stageBoost:0;activeAvatar.root.rotation.x=0;activeAvatar.root.position.y=floorY+.06+((v.restless||emergencyStage>0)?Math.sin(t*2.6)*.003*eff:0);applyClinicalPose(activeAvatar,caseData.id,{...v,emergencyStage,restless:(v.restless||emergencyStage>0)&&eff>.6,headache:v.headache&&eff>.7,guarding:v.guarding&&eff>.65},t);applySkinVisual(activeAvatar,caseData.id,t,treatmentBlend)}const frame=computeView(currentView);desiredCamPos.lerp(frame.pos,.28);desiredTarget.lerp(frame.target,.28);controls.minDistance=frame.min;controls.maxDistance=frame.max}camera.position.lerp(desiredCamPos,.18);controls.target.lerp(desiredTarget,.18);controls.update();renderer.render(scene,camera)}requestAnimationFrame(animate);
addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});newCase();
</script>
</body></html>