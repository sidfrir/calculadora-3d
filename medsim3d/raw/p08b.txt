(){if(!caseData)return;progress.textContent=Math.min(100,Math.round(asked.size/caseData.questions.length*35+examined.size/caseData.exams.length*25+studied.size/caseData.studies.length*20+(selected?20:0)))+'%'}
function getCaseClues(id){return({pneumonia:['respiración rápida','aspecto febril'],asthma:['trabajo respiratorio','inquietud'],['renal-colic']:['inquietud','facies de dolor'],gastroenteritis:['decaimiento','aspecto seco'],appendicitis:['evita moverse','protección abdominal'],migraine:['ojos entrecerrados','evita la luz'],cystitis:['estado general conservado'],['acute-coronary']:['palidez','sudor frío','dolor torácico'],hypoglycemia:['temblor','sudoración','confusión'],anaphylaxis:['urticaria','edema facial','disnea'],stroke:['asimetría facial','disartria','debilidad focal']})[id]||['observación clínica']}
function updateClues(){if(!cluesEl||!caseData)return;cluesEl.innerHTML=getCaseClues(caseData.id).map(t=>`<span class="clue">${t}</span>`).join('')}
function suggestViewForExam(id){if(['lung'].includes(id))return 'chest';if(['abdomen'].includes(id))return 'abdomen';if(['lumbar','skin','general'].includes(id))return 'full';if(['neuro','neck'].includes(id))return 'face';return currentView}
function updateScore(delta=0){caseScore=Math.max(0,Math.min(100,caseScore+delta));if(scorePill)scorePill.textContent=`${caseScore} pts`}
function currentVitals(){if(!caseData?.emergency||emergencyStage===0)return caseData.vitals;const arr=caseData.deterioration?.vitals||[];return arr[Math.min(emergencyStage-1,arr.length-1)]||caseData.vitals}
function updateEmergencyUI(now=performance.now()/1000){if(!timerPill)return;if(!caseData?.emergency){timerPill.hidden=true;return}timerPill.hidden=false;if(stabilized||treated){timerPill.textContent='✓ PRIORIZADO';timerPill.className='timerpill safe';return}const d=caseData.deterioration,elapsed=Math.max(0,now-caseStartedAt),next=(emergencyStage+1)*d.interval,remaining=Math.max(0,Math.ceil(next-elapsed));if(emergencyStage>=d.statuses.length){timerPill.textContent='⚠ CRÍTICO';timerPill.className='timerpill critical'}else{timerPill.textContent=`⚡ PRIORIDAD · ${String(Math.floor(remaining/60)).padStart(2,'0')}:${String(remaining%60).padStart(2,'0')}`;timerPill.className='timerpill'}}
function 