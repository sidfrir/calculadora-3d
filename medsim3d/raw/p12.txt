te una voz para cada sexo. Tocá Probar hasta encontrar una voz realmente masculina y una femenina. La selección queda guardada en este dispositivo.</p><div class="voice-row"><label>Paciente masculino</label><select id="maleVoiceSelect">'+voiceOptionsHtml('Masculino')+'</select><button class="voice-preview" data-preview="Masculino">▶ Probar voz masculina</button></div><div class="voice-row"><label>Paciente femenino</label><select id="femaleVoiceSelect">'+voiceOptionsHtml('Femenino')+'</select><button class="voice-preview" data-preview="Femenino">▶ Probar voz femenina</button></div><div class="finding">Voces detectadas: <b>'+ttsVoices.length+'</b> · en español: <b>'+spanish.length+'</b>.<br>Si todas las voces españolas de tu teléfono suenan femeninas, el navegador no dispone de una voz masculina real. En ese caso necesitás habilitar/instalar una voz TTS masculina en Android y luego volver a abrir este menú.</div>';}
}
document.querySelectorAll('[data-open]').forEach(b=>b.addEventListener('click',()=>openPanel(b.dataset.open)));
document.querySelector('#close').addEventListener('click',closePanel);document.querySelector('#reroll').addEventListener('click',newCase);timerPill?.addEventListener('click',()=>{if(caseData?.emergency)openPanel('urgent')});backdrop.addEventListener('click',e=>{if(e.target===backdrop)closePanel()});
content.addEventListener('change',e=>{if(e.target.id==='maleVoiceSelect'){saveVoicePref('Masculino',e.target.value)}if(e.target.id==='femaleVoiceSelect'){saveVoicePref('Femenino',e.target.value)}});
content.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;
 if(b.dataset.preview){speakWithSex(b.dataset.preview==='Masculino'?'Hola doctor, soy el paciente masculino. Esta es mi voz de prueba.':'Hola doctora, soy la paciente femenina. Esta es mi voz de prueba.',b.dataset.preview);return}
 if(b.dataset.q){const q=caseData.questions.find(x=>x[0]===b.dataset.q);asked.add(q[0]);say(q[2],true);renderPanel('questions');updateProgress()}
 if(b.dataset.e){const ex=caseData.exams.find(x=>x[0]===b.dataset.e);examined.add(ex[0]);setView(suggestViewForExam(ex[0]),true);say(ex[2]);renderPanel('exam');const f=document.querySelector('#finding');f.className='finding';f.textContent=ex[2];updateProgress()}
 if(b.dataset.s){const st=caseData.studies.find(x=>x[0]===b.dataset.s);if(!studied.has(st[0]))updateScore(-1);studied.add(st[0]);say(st[2]);renderPanel('studies');const f=document.querySelector('#finding');f.className='finding';f.textContent=st[2];updateProgress()}
 if(b.dataset.d){selected=b.dataset.d;renderPanel('diagnosis');updateProgress()}
 if(b.id==='confirm'){const r=document.querySelector('#result');if(selected===caseData.diagnosis){diagnosed=true;r.className='result correct';r.innerHTML=`✅ <b>Diagnóstico correcto:</b> ${caseData.diagnosis}.<br><button class="confirm" id="goTreatment" style="margin-top:12px">Continuar al tratamiento →</button>`}else{updateScore(-12);r.className='result wrong';r.innerHTML='⚠️ <b>No coincide con este caso.</b> Revisá signos, exploración y estudios antes de volver a intentar.'}}
 if(b.id==='goTreatment')renderPanel('treatment');
 if(b.dataset.u){const idx=Number(b.dataset.u),opt=urgentOptions()[idx];if(!opt)return;const [label,ok,feedback]=opt;if(ok){stabilized=true;statusText.textContent='Priorizado · continuar evaluación';say(feedback,true);updateEmergencyUI();renderPanel('urgent');const r=document.querySelector('#result');r.className='result correct';r.textContent='✅ '+feedback}else{if(!wrongUrgent.has(idx)){wrongUrgent.add(idx);updateScore(-8)}say(feedback);renderPanel('urgent');const r=document.querySelector('#result');r.className='result wrong';r.textContent='⚠️ '+feedback}}
 if(b.dataset.t){const idx=Number(b.dataset.t),opt=treatmentOptions()[idx];if(!opt)return;const [label,ok,feedback]=opt,r=document.querySelector('#result');if(ok){treated=true;stabilized=true;treatmentBlend=0;updateScore(5);statusText.textContent='En tratamiento · evolución favorable';updateEmergencyUI();say(feedback,true);r.className='result correct';r.innerHTML=`✅ <b>Buena conducta inicial.</b> ${feedback}<div class="finding" style="margin-top:10px"><b>Plan educativo orientativo:</b><ul style="margin:8px 0 0 18px">${caseData.management.map(x=>`<li>${x}</li>`).join('')}</ul></div><div class="finding"><b>Puntaje del caso:</b> ${caseScore}/100</div><button class="nextcase" id="nextCase">Siguiente paciente ↻</button>`}else{if(!wrongTreatments.has(idx)){wrongTreatments.add(idx);updateScore(-10)