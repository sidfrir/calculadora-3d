ion speakWithSex(text,sex){try{speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);const voice=pickPatientVoice(sex);const manual=!!findPreferredVoice(sex);if(voice){u.voice=voice;u.lang=voice.lang||'es-AR'}else u.lang='es-AR';u.rate=sex==='Masculino' ? .89 : .96;u.pitch=manual ? (sex==='Masculino' ? .94 : 1.04) : (sex==='Masculino' ? .58 : 1.07);u.volume=1;u.onstart=()=>{talkUntil=Math.max(talkUntil,performance.now()/1000+Math.max(1.1,Math.min(4.6,text.length*.055)))};u.onend=()=>{talkUntil=Math.max(talkUntil,performance.now()/1000+.15)};speechSynthesis.speak(u)}catch{}}
function speak(text){speakWithSex(text,caseData?.sex||'Femenino')}
function say(text,voice=false){bubble.textContent=text;talkUntil=Math.max(talkUntil,performance.now()/1000+Math.max(1.15,Math.min(4.2,text.length*.05)));if(voice)speak(text)}
function openPanel(type){activePanelType=type;backdrop.classList.add('open');renderPanel(type)}
function closePanel(){activePanelType='';backdrop.classList.remove('open')}
function newCase(){asked.clear();examined.clear();studied.clear();wrongTreatments.clear();wrongUrgent.clear();selected='';diagnosed=false;treated=false;stabilized=false;emergencyStage=0;treatmentBlend=0;caseScore=100;updateScore(0);caseData=makeCase();caseStartedAt=performance.now()/1000;caseNumber++;patientChip.textContent=`${caseData.name}, ${caseData.age}`;statusText.textContent=caseData.status;caseLabel.textContent=`Caso ${String(caseNumber).padStart(3,'0')} · ${caseData.emergency?'emergencia':'aleatorio'}`;say(caseData.opening);updateProgress();closePanel();lastCough=0;setView(currentView,true);applyVisual();updateEmergencyUI()}
function renderPanel(type){activePanelType=type;
 if(type==='questions') content.innerHTML='<h2>Interrogatorio</h2><p class="hint">El caso cambia en cada paciente. Buscá datos que discriminen diagnósticos.</p><div class="list">'+caseData.questions.map(([id,q])=>`<button data-q="${id}" class="${asked.has(id)?'done':''}">${asked.has(id)?'✓ ':''}${q}</button>`).join('')+'</div>';
 if(type==='vitals') content.innerHTML='<h2>Signos vitales</h2><p class="hint">Valores actuales del paciente.</p><div class="vitals">'+currentVitals().map(([k,v,s])=>`<article class="vital ${s}"><span>${k}</span><b>${v}</b></article>`).join('')+'</div>';
 if(type==='exam') content.innerHTML='<h2>Exploración física</h2><p class="hint">Algunos datos solo aparecen si los buscás. También podés cambiar la vista a rostro, tórax, abdomen o cuerpo para reforzar la observación clínica.</p><div class="list">'+caseData.exams.map(([id,label])=>`<button data-e="${id}" class="${examined.has(id)?'done':''}">${examined.has(id)?'✓ ':''}${label}</button>`).join('')+'</div><div id="finding"></div>';
 if(type==='studies') content.innerHTML='<h2>Estudios complementarios</h2><p class="hint">Pedí estudios cuando te ayuden a confirmar o descartar. No todos son imprescindibles, pero aportan contexto.</p><div class="list">'+caseData.studies.map(([id,label])=>`<button data-s="${id}" class="${studied.has(id)?'done':''}">${studied.has(id)?'✓ ':''}${label}</button>`).join('')+'</div><div id="finding"></div>';
 if(type==='diagnosis') content.innerHTML='<h2>Diagnóstico presuntivo</h2><p class="hint">Las opciones cambian y se mezclan en cada caso. Si querés, apoyate en los estudios complementarios antes de decidir.</p><div class="choices">'+caseData.options.map(o=>`<button data-d="${o}" class="${selected===o?'selected':''}">${o}</button>`).join('')+'</div><button class="confirm" id="confirm" '+(selected?'':'disabled')+'>Confirmar diagnóstico</button><div id="result"></div>';
 if(type==='treatment') content.innerHTML='<h2>Tratamiento inicial</h2><p class="hint">Elegí la conducta inicial más razonable para este caso simulado.</p><div class="choices">'+treatmentOptions().map(([label,ok],i)=>`<button data-t="${i}" class="${wrongTreatments.has(i)?'treat-bad':''}">${label}</button>`).join('')+'</div><div id="result"></div>';
 if(type==='urgent') content.innerHTML='<h2>⚡ Prioridad inmediata</h2><p class="hint">Este caso es tiempo-dependiente. Elegí qué priorizar antes de completar el diagnóstico.</p><div class="choices">'+urgentOptions().map(([label,ok],i)=>`<button data-u="${i}" class="${wrongUrgent.has(i)?'treat-bad':''}">${label}</button>`).join('')+'</div><div id="result"></div>';
 if(type==='voices'){refreshVoices();const spanish=ttsVoices.filter(v=>(v.lang||'').toLowerCase().startsWith('es'));content.innerHTML='<h2>🎙️ Voces del paciente</h2><p class="hint">Elegí manualmen